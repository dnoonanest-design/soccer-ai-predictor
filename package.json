{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "pnpm --filter @workspace/api-server run start",
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "railway:build": "pnpm --filter soccer-ai-predictor-api-bundled run build",
    "railway:start": "node --enable-source-maps api-server/dist/index.mjs",
    "migrate": "psql \"$DATABASE_URL\" -f db/001_prediction_platform.sql",
    "healthcheck": "node ./scripts/railway-healthcheck.mjs"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "packageManager": "pnpm@9.15.9"
}
