[phases.setup]
nixPkgs = ["nodejs_22", "pnpm", "postgresql"]

[phases.install]
cmds = ["corepack enable", "corepack prepare pnpm@9.15.9 --activate", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run railway:build"]

[start]
cmd = "pnpm run railway:start"
